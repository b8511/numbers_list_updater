# numbers_list_updater

the flow
websockets gives update -> app receives the message updates bd and sends SSE notification to frontend -> frontend receives SSE notification and updates table accordantly
